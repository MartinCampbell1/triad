# Deprecated Interface

`triad/tui/` is a deprecated standalone interface and is not the mainline desktop experience.

Treat it as historical reference unless the project explicitly revives it as a separate product surface.
