# Archived Path

`triad/patcher/` is an archived compatibility path and does not define current desktop product behavior.

Do not add new desktop features here. Mainline work belongs in:

- `desktop/`
- `triad/desktop/`
- `triad/core/`
