# Legacy Proxy

This directory is legacy and is not part of the canonical desktop release path.

Do not add new desktop dependencies here. Use `desktop/` and `triad/desktop/` for current product work.
