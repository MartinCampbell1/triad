# Legacy Path

`triad/proxy/` is not part of the mainline desktop product.

Use these paths only for historical reference or targeted experiments. New desktop work should land in:

- `desktop/`
- `triad/desktop/`
- `triad/core/`
